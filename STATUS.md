# Rebuild Status

**Status: Rebuild in progress — do not treat this as a finished project.**

This repository is undergoing a structured remediation. A review surfaced 15
findings covering broken dependencies, a non-functional live demo, missing tests,
spoofable auth, and documentation that described aspirational features rather than
working code. The archive tag `v0-portfolio-archive` preserves the prior state.
The `rebuild/` branch contains all new work; `main` will be updated only when a
phase reaches its exit criteria. Check back once the rebuild is complete.
